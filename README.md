# l-vlPAG_ActiveAvoidance

The included code was used to convert the DeepLabCut output (.h5 file) to a 'Tracking.mat' file with angle, position, velocity, acceleration, etc. 
as well as behavior categorization.
